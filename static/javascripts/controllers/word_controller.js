Wordy.WordController = Ember.ObjectController.extend({
    // nextWord : function(){
    //     var words = Wordy.Word.find();
    //     var word  = this.get("model");
    //     var next_index = words.indexOf(word) + 1;
    //     next_index = next_index >= words.get('length') ? 0 : next_index;

    //     return words.objectAt(next_index);
    // },previousWord : function(){
    //     var words = Wordy.Word.find();
    //     var word  = this.get("model");
    //     var previous_index = words.indexOf(word) - 1;
    //     previous_index = previous_index < 0 ? words.get('length') - 1 : previous_index;

    //     return words.objectAt(previous_index);
    // }
});